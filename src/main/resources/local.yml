spring:
  datasource:
    url: jdbc:h2:mem:local-db;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
  session:
    store-type: redis
    redis:
      namespace: jjabtwit-session
  data:
    redis:
      host: localhost
      password: 1234
      port: 6379
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 40MB

server:
  servlet:
    session:
      cookie:
        name: JSESSIONID

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace

salt:
  en1: locals1
  en2: localS2

image:
  root-dir: src/main/resources/static/images/post/

